import Columns from "@/components/columns";

export default function Home() {
  return (
    <section className="flex h-screen bg-gradient-to-t">
      <div>
        {/* columns */}
        <Columns />
      </div>
    </section>
  );
}
